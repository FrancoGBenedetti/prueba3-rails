<p id="notice"><%= notice %></p>

<h1>Empresas</h1>

<!--<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @empresas.each do |empresa| %>
      <tr>
        <td><%= empresa.title %></td>
        <td><%= link_to 'Show', empresa %></td>
        <td><%= link_to 'Edit', edit_empresa_path(empresa) %></td>
        <td><%= link_to 'Destroy', empresa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>-->

<br>




<div class="container">
  <div class="row">
    <div class="col-md-6">

      <ul id="empresas">
        <% @empresas.each do |empresa| %>

           <li id= '<%= empresa.id  %>' >
             <ul>
                <li>
                  <%= empresa.title %>
                </li>
                <li>
                  <%= link_to 'Destroy', empresa, method: :delete, data: {remote: true} %>
                </li>
                <li>
                  <%= link_to 'Edit', edit_empresa_path(empresa), data: {remote: true} %>
                </li>
               
             </ul> 
            
            
            

           </li>

        <% end %>
      </ul>

    </div>

    <div class="col-md-6">
    


      <%= form_with(model: @empresa) do |form| %>
        

        <div class="field">
          <%= form.label :title %>
          <%= form.text_field :title, id: :empresa_title %>
        </div>

        <div class="actions">
          <%= form.submit %>
        </div>
      <% end %>
      
    </div>

  </div>
</div>

<!-- Button trigger modal -->


<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="modal-body">
        
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>
